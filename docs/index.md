# conlens

`conlens` detects whether a predefined connection set accumulates near either end
of a complete signed edge ranking, then extracts the connections that drive the
observed enrichment. The implementation keeps the deterministic statistic separate
from explicitly selected null inference.

Start with [Concepts](concepts.md), then follow [How LENS works](algorithm.md) and
the runnable [Tutorials](tutorials.md).
